{
  "name": "steam-family-new-game-notification",
  "version": "1.0.0",
  "private": true,
  "description": "Steam Family Sharing library change notifier",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check-new-games": "node dist/cli/check-new-games.js",
    "backfill-purchase": "node dist/cli/backfill-purchase.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
